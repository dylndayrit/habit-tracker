export default function HabitList({habits, onDelete, onUpdate, defaultUserId = 1}) {
    const [status, ]
    return (
        <div className="card">
            <ul>
                <li></li>

      <div style={{ display: "flex", flexWrap: "wrap", gap: 8 }}>{children}</div>
    
            </ul>
        </div>
    );
};